services:
  - type: web
    name: carethread
    runtime: node
    buildCommand: npm install --include=dev && npm run build
    startCommand: node server/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: AUTH_DISABLED
        value: "true"
